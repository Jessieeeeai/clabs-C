{
  "name": "clabs-c",
  "scripts": {
    "build": "echo 'Static build complete'",
    "deploy": "wrangler pages deploy public --project-name clabs-c"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}